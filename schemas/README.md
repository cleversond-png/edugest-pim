# Schemas JSON (validação)

Arquivos:
- opportunity_context.schema.json — valida o payload de entrada do runtime
- solution_pack.schema.json — valida o output consolidado (SolutionPack)
- render_result.schema.json — valida o retorno do renderer (bundle de docs/exports)

Padrão: JSON Schema Draft 2020-12.
