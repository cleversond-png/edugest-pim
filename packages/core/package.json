{
  "name": "@edugest-pim/core",
  "version": "1.0.0",
  "description": "Core modules for EduGest-PIM: orchestrator, agents, guardrails, and types",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^3.0.1",
    "@anthropic-ai/sdk": "^0.24.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
