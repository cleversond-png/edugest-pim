name: Deploy to Azure Container Apps

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    env:
      AZURE_RESOURCE_GROUP: rg-edugest-pim-prod
      ACR_NAME: acredgestpimprod
      ACR_LOGIN_SERVER: acredgestpimprod.azurecr.io
      API_CONTAINER_APP: ca-edugest-pim-api
      WEB_CONTAINER_APP: ca-edugest-pim-web-prod
      API_URL: https://ca-edugest-pim-api.purpleground-cde5672b.brazilsouth.azurecontainerapps.io

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build Core package
        run: npm run build --workspace=@edugest-pim/core

      - name: Run linter
        run: npm run lint --workspace=web || true

      - name: Run tests (API)
        run: npm run test --workspace=@edugest-pim/api || true

      - name: Build API
        run: npm run build --workspace=@edugest-pim/api

      - name: Build Web (standalone)
        run: npm run build --workspace=web
        env:
          NEXT_PUBLIC_API_URL: ${{ env.API_URL }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'

      - name: Azure Login
        uses: azure/login@v1
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Login to Azure Container Registry
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: az acr login --name ${{ env.ACR_NAME }}

      - name: Build and push backend image
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          docker buildx build \
            --platform linux/amd64 \
            --push \
            -t ${{ env.ACR_LOGIN_SERVER }}/edugest-pim-api:${{ github.sha }} \
            -t ${{ env.ACR_LOGIN_SERVER }}/edugest-pim-api:latest \
            .

      - name: Build and push frontend image
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          docker buildx build \
            --platform linux/amd64 \
            --push \
            -f apps/web/Dockerfile \
            -t ${{ env.ACR_LOGIN_SERVER }}/edugest-pim-web:${{ github.sha }} \
            -t ${{ env.ACR_LOGIN_SERVER }}/edugest-pim-web:latest \
            .

      - name: Deploy backend Container App
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          az containerapp update \
            --name ${{ env.API_CONTAINER_APP }} \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --image ${{ env.ACR_LOGIN_SERVER }}/edugest-pim-api:${{ github.sha }}

      - name: Configure frontend secrets
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          az containerapp secret set \
            --name ${{ env.WEB_CONTAINER_APP }} \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --secrets api-key=${{ secrets.API_KEY }}

      - name: Deploy frontend Container App
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          az containerapp revision set-mode \
            --name ${{ env.WEB_CONTAINER_APP }} \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --mode single

          az containerapp update \
            --name ${{ env.WEB_CONTAINER_APP }} \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --image ${{ env.ACR_LOGIN_SERVER }}/edugest-pim-web:${{ github.sha }} \
            --set-env-vars \
              NODE_ENV=production \
              PORT=3001 \
              HOSTNAME=0.0.0.0 \
              API_URL=${{ env.API_URL }} \
              NEXT_PUBLIC_API_URL=${{ env.API_URL }} \
              API_KEY=secretref:api-key \
              FRONTEND_BUILD=${{ github.sha }}

      - name: Production smoke test
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: bash scripts/qa-frontend-production.sh https://${{ env.WEB_CONTAINER_APP }}.purpleground-cde5672b.brazilsouth.azurecontainerapps.io

      - name: Azure Logout
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: az logout || true

  notify:
    runs-on: ubuntu-latest
    needs: build-and-deploy
    if: always()

    steps:
      - name: Deployment Status
        run: |
          echo "Deployment Result: ${{ needs.build-and-deploy.result }}"
          if [ "${{ needs.build-and-deploy.result }}" == "success" ]; then
            echo "✅ Deployment successful! Backend and frontend Container Apps updated."
          else
            echo "⚠️  Check deployment logs above."
          fi
